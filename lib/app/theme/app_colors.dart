import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Glowella Brand Colors
  static const Color primary = Color(0xFF4CAF8C);        // Green
  static const Color primaryLight = Color(0xFF7FCFB0);
  static const Color primaryDark = Color(0xFF2E8B6E);
  static const Color background = Color(0xFFE8F5F0);     // Mint green bg
  static const Color cardBackground = Colors.white;
  static const Color textPrimary = Color(0xFF2C3E35);    // Dark green-black
  static const Color textSecondary = Color(0xFF5A7A6A);
  static const Color textLight = Colors.white;
  static const Color textMuted = Color(0xFF9EB8AE);

  // Routine Card Colors
  static const Color routineMaster = Color(0xFFA8D5C2);   // Soft green
  static const Color routineMorning = Color(0xFFFFE4A0);  // Warm yellow
  static const Color routineNight = Color(0xFFB8A0D4);    // Soft purple

  // Status / Utility
  static const Color success = Color(0xFF4CAF8C);
  static const Color warning = Color(0xFFFFB74D);
  static const Color danger = Color(0xFFE57373);
  static const Color info = Color(0xFF64B5F6);

  // Neutral
  static const Color divider = Color(0xFFD5EAE0);
  static const Color shadow = Color(0x1A4CAF8C);
  static const Color shimmer = Color(0xFFEAF5F0);

  // Bottom Nav
  static const Color navActive = Color(0xFF4CAF8C);
  static const Color navInactive = Color(0xFFB0C8BE);
}
